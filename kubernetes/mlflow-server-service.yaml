apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: mlflow-server
  name: mlflow-server
spec:
  ports:
    - name: "5001"
      port: 5001
      targetPort: 6000
  selector:
    io.kompose.service: mlflow-server
