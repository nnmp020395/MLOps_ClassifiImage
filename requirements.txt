# dépendances pour le fonctionnement de airflow avec mlflow
mlflow==2.7.0

# dépendances pour le fonctionnement de airflow avec postgres
psycopg2-binary==2.9.10
sqlalchemy==1.4.52

# dépendances pour le fonctionnement de airflow avec AWS
boto3==1.36.12
apache-airflow-providers-amazon==9.2.0

# dépendances pour les requetes http
requests==2.31.0

# dépendances pour les providers communs
apache-airflow-providers-common-io==1.3.0

pandas==2.2.3
python-dotenv==0.19.0

#pendulum==2.1.2

# Add pyarrow with a version that has pre-built binaries, and compatible with mlflow
pyarrow>=4.0.0,<14

setuptools==75.8.0 
wheel==0.45.1

